# CameraConnect
---
## VS Code → Right click → Open with Live Server
## It is impossible to access the camera from a plain local HTML file (file://) in modern browsers.
## chrome.exe --unsafely-treat-insecure-origin-as-secure="file://"
## Modern browsers block camera access for security reasons unless the page is loaded from:
http://localhost or https://

---
